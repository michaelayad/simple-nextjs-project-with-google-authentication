/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakeimg.pl','lh3.googleusercontent.com'], // Add the hostname here
      },
};

export default nextConfig;
